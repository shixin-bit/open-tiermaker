## Purpose

提供排行榜的核心画布，包括等级行（S/A/B/C/D）的渲染、自定义等级标签名称与颜色、以及整体布局管理。

## Requirements

### Requirement: 默认等级行展示

系统 SHALL 在页面加载时展示五个固定等级行，标签从 top 到 bottom 依次为 S、A、B、C、D，每个等级行带有对应的默认颜色。

#### Scenario: 页面首次加载

- **WHEN** 用户打开应用页面
- **THEN** 页面显示五个等级行，左侧标签列显示 S/A/B/C/D 文字及对应颜色，右侧内容区域为空等待图片放置

### Requirement: 自定义等级标签名称

用户 SHALL 能够点击等级标签进行编辑，修改等级的显示名称。修改后名称 SHALL 立即生效并在页面刷新后通过浏览器本地存储持久化。

#### Scenario: 修改等级名称

- **WHEN** 用户双击 S 等级的标签并输入 "神级" 然后点击外部确认
- **THEN** 该等级标签显示为 "神级"，页面刷新后仍然保持

#### Scenario: 名称校验

- **WHEN** 用户将等级名称清空或输入超过 20 个字符
- **THEN** 系统拒绝修改并恢复原来的名称

### Requirement: 自定义等级颜色

用户 SHALL 能够通过颜色选择器修改每个等级行的标签背景色。修改后颜色 SHALL 立即生效并持久化。

#### Scenario: 修改等级颜色

- **WHEN** 用户点击 A 等级的颜色区域并选择一个新颜色
- **THEN** A 等级的标签背景色变为新选择的颜色，页面刷新后仍然保持

### Requirement: 等级行顺序固定

等级行的垂直顺序 SHALL 固定为从上到下 S → A → B → C → D，用户不可调整等级行顺序。

#### Scenario: 尝试调整等级顺序

- **WHEN** 用户尝试拖动等级标签到其他位置
- **THEN** 等级行顺序保持不变，不响应拖动操作

### Requirement: 图片池区域

系统 SHALL 在等级行下方提供一个"图片池（Pool）"区域，用于存放未分配到任何等级的图片。

#### Scenario: 图片池展示

- **WHEN** 用户打开应用且没有添加任何图片
- **THEN** 页面底部显示一个空的图片池区域，带有提示文字引导用户添加图片
